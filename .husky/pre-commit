#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Executa o lint (eslint + prettier) e os testes unitários (vitest) antes de cada commit.
# Se um dos comandos falhar (ex: linting falha ou testes falham), o commit é abortado.
npm run lint
npm run test:unit 